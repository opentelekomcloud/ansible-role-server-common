---
dependency:
  name: galaxy
driver:
  name: vagrant
lint:
  name: yamllint
platforms:
  - name: fedora30-vm
    box: generic/fedora30
provisioner:
  name: ansible
  lint:
    name: ansible-lint
verifier:
  name: testinfra
  lint:
    name: flake8
